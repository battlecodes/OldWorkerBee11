<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Variables.xaml"/>
    </ResourceDictionary.MergedDictionaries>


    <!-- Default styles -->
    <Style x:Key="DefaultButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="11,5,11,6"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Border.CornerRadius" Value="{StaticResource ControlCornerRadius}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DefaultButtonImageStyle" TargetType="Image">
        <Setter Property="Height" Value="15"/>
        <Setter Property="Margin" Value="0,5,15,5"/>
    </Style>

    <Style x:Key="DefaultButtonLabelStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>
    
</ResourceDictionary>